import { IonCard, IonCardContent, IonPopover, PopoverOptions, UseIonPopoverResult } from '@ionic/react';


const accountSettings: React.FC = () => {
return (
     <IonPopover>
        <IonCard>
            <IonCardContent>
                Hello
                </IonCardContent></IonCard></IonPopover>

    );
};

export default accountSettings;